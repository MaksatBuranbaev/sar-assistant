# Прямые зависимости проекта. Транзитивные не перечисляются намеренно:
# файл читают люди, а воспроизводимость сборки обеспечивает pip resolver
# по этим границам версий.

# Интерфейс
streamlit==1.55.0
pandas==2.3.3

# Языковая модель
gigachat==0.1.38
tenacity==9.1.4          # повтор сетевых вызовов с экспоненциальной задержкой
httpx==0.27.2            # используется gigachat; типы исключений нужны для retry

# База данных
psycopg[binary]==3.2.4
psycopg-pool>=3.2,<4

# Валидация SQL и работа с примерами
sqlglot>=25,<31          # разбор SQL в AST вместо проверок регулярками
PyYAML>=6,<7             # банк few-shot примеров и eval-датасет

# Конфигурация
python-dotenv==1.0.1
